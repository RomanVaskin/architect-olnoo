# Database

**Version:** 0.2.0
**Status:** Draft migration prepared — not applied
**Last Updated:** 2026-07-20

> Описывает схему данных, модели и решения по хранению данных проекта Architect OLNOO.

---

## Назначение документа

Этот документ фиксирует структуру базы данных: сущности, связи между ними, выбор СУБД и стратегию миграций.

## Подготовленная схема

Предварительная миграция находится в `supabase/migrations/202607200001_backend_foundation.sql` и пока не применена к удалённому проекту. Она включает:

- workspaces и роли участников (`owner`/`editor`/`viewer`);
- проекты, исходные файлы и подтверждённые ракурсы;
- концепции, связанные исправленные версии и историю версий;
- попытки AI-генерации и события проекта;
- приватный Storage bucket `project-assets`;
- RLS-политики, ограничивающие данные участниками соответствующего workspace/project.

Бинарные файлы не хранятся в PostgreSQL: таблица `project_files` содержит только метаданные и путь, а реальные байты размещаются в приватном Supabase Storage по схеме `<workspace_id>/<project_id>/<opaque_file_name>`.

## До применения

Необходимо выбрать точный Supabase-проект, проверить существующую схему и конфликты имён, просмотреть все RLS-политики и явно подтвердить миграцию. До этого IndexedDB остаётся рабочим локальным MVP-хранилищем.

## Остаётся разработать

- ER-диаграмма
- Стратегия миграций
